import * as area from './1.0-CalcArea';
console.log(area.circleArea(2));
console.log(area.square(2));

export const triArea = (4 * 2) / 2

export default class Book {
    constructor(title) {
        this.title = title;
    }
    printTitle() {
        console.log(this.title);
    }
}